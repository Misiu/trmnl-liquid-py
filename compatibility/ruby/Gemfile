source "https://rubygems.org"

# Liquid 5.13 requires base64, which stopped being a Ruby default gem in Ruby 3.4.
# These are oracle-only dependencies; they are not runtime dependencies of trmnl-liquid-py.
gem "base64", "~> 0.3"
gem "i18n", "~> 1.14"
gem "trmnl-liquid", "= 0.8.2"
